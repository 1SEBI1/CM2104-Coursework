<!DOCTYPE html>
<html>

<head>
  <% include ../partials/head %>
  <title>Edit account - Cookie Jar</title>
</head>

<body>

  <header>
    <% include ../partials/header %>
  </header>

  <main id="mainbody">
    <div class='box'>

      <!-- form with entry box for each bit of inomfrtaion we want from our user. -->
      <h3>Modify your profile</h3>
      <form action="/edit" method="POST">
        <label>Name</label>
        <input type="text" placeholder=" <%= user.name.first %>" name="first">
        <input type="text" placeholder=" <%= user.name.last %>"  name="last">
      </br><label>Login Details</label>
        <input type="text" placeholder=" <%= user.login.username %>"     name="username">
      </br><label>Extra Details</label>
        <input type="email" placeholder=" <%= user.email %>"    name="email">
        <input type="datetime" placeholder=" <%= user.dob %>"    name="dob">
        </br><button type="submit">Submit</button>
      </form>


    </div>
  </main>
  <footer>
    <% include ../partials/footer %>
  </footer>

</body>

</html>
